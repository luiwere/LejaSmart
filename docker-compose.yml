version: '3.8'
services:
  lejasmart:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data/lejasmart.db:/app/lejasmart.db
      - ./data/lejasmart_owner.db:/app/lejasmart_owner.db
    restart: unless-stopped
